import React from 'react'
import './styles.scss';
import { Button } from 'react-bootstrap';

const StatusButton = ({url}) => {

    return <Button className="status-button">
        En ejecución
    </Button>
}

export default StatusButton;